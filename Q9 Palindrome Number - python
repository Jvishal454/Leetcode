class Solution:
    def isPalindrome(self, x: int) -> bool:
        
        rev = 0
        temp = x
        
        while x>0:
            digit = x % 10
            rev = rev * 10 + digit
            x = int(x / 10)
        return 1 if temp == rev else 0
